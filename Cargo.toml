[package]
name = "bottlerocket-bootstrap-associate-eip"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = "0.48.0"
aws-sdk-ec2 = "0.18.0"
env_logger = "0.9.0"
http = "0.2.8"
tokio = { version = "1", features = ["macros"] }
